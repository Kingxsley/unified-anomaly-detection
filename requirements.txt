# Streamlit for building the interactive dashboard
streamlit==1.17.0

# Pandas for data manipulation and handling
pandas==1.3.3

# Plotly for visualizations and interactive charts
plotly==5.3.1

# InfluxDB client for querying and interacting with InfluxDB time-series data
influxdb-client==1.21.0

# SQLiteCloud for storing and querying data in SQLite Cloud
sqlitecloud==0.3.0

# Requests for making HTTP requests (for example, to send Discord alerts)
requests==2.26.0

# Scikit-learn for machine learning functionality (anomaly detection, etc.)
scikit-learn==0.24.2

# NumPy for numerical operations and data handling
numpy==1.21.2

# Markdown-it-py for handling Markdown rendering in Streamlit (if used)
markdown-it-py==3.0.0

# MDUrl for URL parsing (dependency used for Markdown rendering)
mdurl==0.1.2

# Rich for beautiful terminal output (used in some parts of the app)
rich==14.0.0
pip install --upgrade pip
pip install markdown-it-py==3.0.0
pip install mdurl==0.1.2
pip install pygments==2.19.2
pip install rich==14.0.0
pip cache purge

